apiVersion: v1
kind: Pod
metadata:
  labels:
    app: jenkins-ci
spec:
  serviceAccountName: default

  containers:
    - name: ci
      image: localhost:32000/jenkins/python-ci:3.12
      imagePullPolicy: IfNotPresent

      command:
        - cat
      tty: true

      resources:
        requests:
          memory: "512Mi"
          cpu: "250m"
        limits:
          memory: "1Gi"
          cpu: "1000m"

  restartPolicy: Never
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-pvc
  # Optional: if you’re using a namespace other than default, add:
  # namespace: jenkins
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
